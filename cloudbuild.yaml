steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/xhiqibot-image', '.']
images:
- 'gcr.io/$PROJECT_ID/xhiqibot-image'
options:
  diskSizeGb: '200' # ディスクサイズは前回増やしたのでこのままでOK
  machineType: 'n1-highmem-8' # <-- この行を追加！高メモリマシンタイプを指定
