steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/xhiqibot-image', '.']
images:
- 'gcr.io/$PROJECT_ID/xhiqibot-image'
options:
  diskSizeGb: '200'         # ディスク容量を増加
  machineType: 'n1-highmem-8' # 高メモリマシンタイプを使用
